<div class="col-md-4">
  <h3>Channels</h3>
  <ul>
    <% @channels.each do |c| %>
      <li class="list-unstyled"><%= link_to c.channel, c %></li>
    <% end %>
  </ul>

  <%= link_to "New channel", new_channel_path, class: "btn btn-info" %>
</div>
